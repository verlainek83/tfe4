extends layout 

block content 
  if user 
    .menu-bar
      ul
        li
          a(href="/adresses/create" class={active: currentUrl === "/adresses/create/new"}) 
            i.fa.fa-save
            |  Enregistrer son parking
        li
          a(href="/parkings" class={active: currentUrl === "/parkings"}) 
            i.fa.fa-ticket
            |  Faire une reservation
        li
          a(href="/places" class={active: currentUrl === "/places"})
            i.fa.fa-ticket
            |  Faire une location
        li
          a(href='/publications')
            i.fa.fa-newspaper-o
            |  Publications
        li.active
          a(href='')
            i.fa.fa-user
            |  #{user.username}
          .sub-menu-1
            ul
              li
                a(href='/account') Profil
              li
                a(href='/mesReservations') Mes reservations
              li
                a(href='') Mes parkings
              li
                a(href='') Dashboard
        li
          a(href='/logout')
            i.fa.fa-sign-out
            |  Se d&eacute;connecter

  h1= codeReservation

  //- if user.can("createReservation")
  //-   .mb-3
  //-     a.nav-link(href="/reservations/create" class={active: currentUrl === "/reservations/create/new"}) Creer une nouvelle reservation


  table.table
    thead
      tr
        th(scope="col") codeReservation
        th(scope="col") dateReservation
        th(scope="col") dateOccupation
        th(scope="col") dateDepart
        th(scope="col") heureDebut
        th(scope="col") heureDepart
        th(scope="col") validationReservation
        th(scope="col") numeroImmatriculation
        th(scope="col") place description
        th(scope="col") &nbsp;
    tbody
              //- each reservation in reservations
    tr
      td #{reservation.userUsername}
      //- td= reservation.codeReservation
      //- td= reservation.dateReservation
      //- td= reservation.dateOccupation
      //- td= reservation.dateDepart
      //- td= reservation.heureDebut
      //- td= reservation.heureDepart
      //- td= reservation.validationReservation
      //- td= reservation.vehicule.numero_immatriculation
      //- td= reservation.place.description
     
  script.
    const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-toggle="tooltip"]'))
    tooltipTriggerList.map(function (tooltipTriggerEl) {
      return new bootstrap.Tooltip(tooltipTriggerEl)
    })