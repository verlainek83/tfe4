extends layout 

block content 
  body(style='background-color:powderblue;')
    center()
      h1 Mise à jour 
      form(action="/trafics/"+trafic.id, method="post")
        .mb-3
          label.form-label(for="nomInput") Changer le nom:
          input#nomInput.form-control(type="text", name="nom", value=trafic.nom )
        .mb-3
          button.btn.btn-primary(type="submit") Save
          | &nbsp;
          a.btn.btn-outline-danger(href="/trafics/"+trafic.id) Cancel