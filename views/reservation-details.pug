extends layout

block content
  body(style='background-color:powderblue;')
    center()
      h1 Location
      //- p Adresse: #{parking.adresse.nom}, #{parking.adresse.numero}
      p Date de début #{reservation.codeReservation}
      p Date de fin #{reservation.dateReservation}
      p Heure de début #{reservation.dateOccupation}
      p Heure de fin #{reservation.dateDepart}
      p Nom du locataire: #{reservation.heureArrivee}
      
      a.nav-link(href="/locations" class={active: currentUrl === "/locations"}) 
        button.btn.btn-outline-success(type='button') Faire une location?